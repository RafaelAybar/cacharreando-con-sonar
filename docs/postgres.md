# Ejemplos de configuración de Postgres

## Fichero .env

En la raíz del proyecto hemos de crear un fichero llamado postgres.env con los siguientes datos:
```
POSTGRES_USER=rafa
POSTGRES_PASSWORD=CambiameInmediatamente
POSTGRES_DB=sonar
```